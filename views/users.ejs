<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Users</title>
</head>

<body>
  List of users:
  <div>
    <ul>
      <% users.forEach(user=> { %>
        <li>
          <%= user.id %>, <%= user.firstName %>, <%= user.lastName %>
        </li>
        <%})%>
    </ul>
  </div>
</body>

</html>